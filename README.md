# 📊 Система анализа и классификации задач JIRA

Этот проект автоматически обрабатывает задачи из JIRA, создает их краткие описания с помощью искусственного интеллекта и классифицирует по категориям для дальнейшего анализа.

## 🎯 Что делает система

1. **Загружает задачи** из JIRA по вашему запросу
2. **Создает краткие описания** каждой задачи с помощью ИИ
3. **Генерирует категории** работ на основе анализа задач
4. **Классифицирует все задачи** по созданным категориям
5. **Сохраняет результаты** в удобные Excel-файлы

## 🛠️ Первоначальная настройка

### Шаг 1: Установка Python

1. Скачайте Python **версии 3.13.3** с официального сайта: https://www.python.org/downloads/
2. При установке **обязательно поставьте галочку** "Add Python to PATH"
3. Перезагрузите компьютер после установки
4. Проверьте установку, выполнив в командной строке:
   ```
   python --version
   ```
   Должно показать: `Python 3.13.3`

### Шаг 2: Подготовка проекта

1. Распакуйте архив с проектом в удобную папку
2. Откройте командную строку (нажмите Win+R, введите `cmd`)
3. Перейдите в папку проекта:
   ```
   cd C:\путь\к\вашей\папке\exp_survey
   ```

### Шаг 3: Создание виртуальной среды (рекомендуется)

**💡 Зачем нужна виртуальная среда?**
Виртуальная среда изолирует зависимости проекта от системных библиотек Python, предотвращая конфликты версий.

**Создание и активация виртуальной среды:**

1. В папке проекта выполните:
   ```
   python -m venv venv
   ```
   Это создаст папку `venv` с виртуальной средой.

2. Активируйте виртуальную среду:
   
   **Для Windows:**
   ```
   venv\Scripts\activate
   ```
   
   **Для Linux/Mac:**
   ```
   source venv/bin/activate
   ```

3. После активации в командной строке должно появиться `(venv)` в начале строки:
   ```
   (venv) C:\путь\к\проекту\exp_survey>
   ```

**⚠️ ВАЖНО:** Каждый раз при работе с проектом сначала активируйте виртуальную среду!

**Деактивация виртуальной среды:**
Когда закончите работу с проектом, выполните:
```
deactivate
```

### Шаг 4: Установка зависимостей

**После активации виртуальной среды** выполните в командной строке:
```
pip install -r requirements.txt
```

### Шаг 5: Настройка подключения к JIRA

1. Найдите файл `.env.example` в папке проекта
2. Скопируйте его и переименуйте в `.env`
3. Откройте файл `.env` в любом текстовом редакторе
4. Заполните следующие параметры:

```
# Настройки JIRA
JIRA_URL=https://ваш-jira-сервер.com
JIRA_TOKEN=ваш-токен-доступа
JIRA_CERT_PATH=путь/к/сертификату.crt  # если нужен сертификат
JIRA_VERIFY_SSL=true  # true или false

# Настройки ИИ (уже настроены для внутреннего сервера)
AI_API_BASE=http://sm-litellm.gksm.local
AI_API_KEY=sk-vNZ2bKnZ0CLG7TvVx3lYOg
AI_MODEL=Cloud.ru/Qwen3-Coder-480B-A35B-Instruct
```

**Где взять токен JIRA:**
1. Войдите в JIRA
2. Перейдите в настройки профиля
3. Найдите раздел "API tokens" или "Токены API"
4. Создайте новый токен и скопируйте его

## 🚀 Запуск системы

### Основной способ запуска

1. Откройте файл `main.py` в любом текстовом редакторе
2. Найдите строку с `JQL = "..."` (примерно строка 18)
3. Замените запрос на ваш:
   ```python
   JQL = "project = ВАША_ЗАДАЧА AND created >= 2024-01-01 ORDER BY created DESC"
   ```

4. Откройте командную строку в папке проекта
5. **Активируйте виртуальную среду** (если используете):
   ```
   venv\Scripts\activate
   ```
6. Выполните команду:
   ```
   python main.py
   ```

### Примеры JQL запросов

```python
# Все задачи проекта за последний месяц
JQL = "project = MYPROJECT AND created >= -30d ORDER BY created DESC"

# Задачи определенного типа
JQL = "project = MYPROJECT AND issuetype = Bug ORDER BY created DESC"

# Задачи с определенным статусом
JQL = "project = MYPROJECT AND status = Done AND updated >= -7d"

# Задачи по исполнителю
JQL = "project = MYPROJECT AND assignee = 'ivan.petrov' ORDER BY created DESC"
```

**⚠️ ВАЖНО:** В JQL запросе обязательно должен быть указан проект (`project = НАЗВАНИЕ`), иначе система выдаст ошибку.

## 📁 Результаты работы

После завершения работы система создаст папку с названием вашего проекта (например, `classification_data/MYPROJECT/`) со следующими файлами:

### 📄 Основные файлы:

1. **`tasks.xlsx`** - Исходные задачи из JIRA
2. **`tasks_summary.xlsx`** - Задачи с краткими описаниями от ИИ
3. **`categories.xlsx`** - Сгенерированные категории работ
4. **`final_categories.xlsx`** - Итоговый список категорий
5. **`classified_tasks.xlsx`** - Задачи с присвоенными категориями

### 📊 Как читать результаты:

**В файле `classified_tasks.xlsx`:**
- Колонка `Category` - присвоенная категория
- Колонка `summary` - краткое описание от ИИ
- Остальные колонки - данные из JIRA

## ⚙️ Настройка параметров

Откройте файл `main.py` и найдите раздел с настройками (примерно строки 25-60):

### 🔧 Основные настройки:

```python
# Какие этапы выполнять (True = выполнять, False = пропустить)
PIPELINE_STEPS = {
    'fetch_tasks': True,        # Загрузка задач из JIRA
    'summarize_tasks': True,    # Создание кратких описаний
    'generate_categories': True, # Генерация категорий
    'consolidate_categories': True, # Объединение категорий
    'classify_tasks': True      # Классификация задач
}

# Сохранять ли файлы с временными метками (для тестирования)
SAVE_TIMESTAMPED_FILES = False

# Максимальное количество задач для обработки (0 = без ограничений)
MAX_RESULTS = 100
```

### ⚡ Настройки производительности:

```python
# Количество одновременно обрабатываемых задач
SUMMARIZATION_THREADS = 10      # Для создания описаний
CATEGORY_GENERATION_THREADS = 10 # Для генерации категорий  
CLASSIFICATION_THREADS = 10     # Для классификации

# Размеры порций для обработки
CATEGORY_GENERATION_BATCH_SIZE = 5  # Задач в одной порции для категорий
CLASSIFICATION_BATCH_SIZE = 15      # Задач в одной порции для классификации
JIRA_CHUNK_SIZE = 50               # Задач загружать за раз из JIRA
```

**💡 Рекомендации:**
- Для медленного интернета уменьшите количество потоков до 3-5
- Для быстрого интернета можно увеличить до 15-20
- Если система "зависает", уменьшите размеры порций

## 🔍 Режимы классификации

```python
# Режим классификации
CLASSIFICATION_MODE = "single"  # "single" или "batch"
```

- **"single"** - обрабатывает задачи по одной (точнее, но медленнее)
- **"batch"** - обрабатывает группами (быстрее, но менее точно)

## 🚨 Решение проблем

### Проблема: "Ошибка 401 Unauthorized"
**Решение:** Проверьте токен JIRA в файле `.env`

### Проблема: "Проект не указан в JQL"
**Решение:** Добавьте `project = НАЗВАНИЕ` в ваш JQL запрос

### Проблема: "Permission denied" при сохранении Excel
**Решение:** Закройте все открытые Excel файлы из папки результатов

### Проблема: Система "зависает"
**Решение:** 
1. Уменьшите количество потоков до 3-5
2. Уменьшите размеры порций
3. Установите ограничение `MAX_RESULTS = 50` для тестирования

### Проблема: Неправильные категории
**Решение:**
1. Используйте режим `CLASSIFICATION_MODE = "single"`
2. Проверьте качество кратких описаний в файле `tasks_summary.xlsx`

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте настройки** в файле `.env`
2. **Попробуйте с меньшим количеством задач** (`MAX_RESULTS = 10`)
3. **Проверьте JQL запрос** в JIRA веб-интерфейсе
4. **Обратитесь к администратору** если проблемы с доступом к JIRA или ИИ

## 🎯 Быстрый старт для нетерпеливых

1. Установите Python 3.13.3
2. Создайте виртуальную среду: `python -m venv venv`
3. Активируйте её: `venv\Scripts\activate` (Windows)
4. Установите зависимости: `pip install -r requirements.txt`
5. Скопируйте `.env.example` в `.env` и заполните настройки JIRA
6. Измените JQL запрос в `main.py`
7. Выполните `python main.py`
8. Ждите результаты в папке `classification_data/`

**Готово!** 🎉

---

*Система протестирована на Windows 10/11 с Python 3.13.3*
*Рекомендуется использование виртуальной среды для изоляции зависимостей*